# 12.6 Cancel Reminder

```http
PUT /api/reminders/{{reminderId}}/cancel
Authorization: Bearer {{token}}
```

Expected:

- Status `200`.
- `data.id` equals `{{reminderId}}`.
- `data.status` is `CANCELLED`.

