# 12.7 Get Notifications

```http
GET /api/notifications
Authorization: Bearer {{token}}
```

Expected:

- Status `200`.
- `data` is an array.

Note:

- This step verifies the notification query API.
- End-to-end reminder-to-notification generation is covered by backend integration tests because Apifox cannot safely move a reminder's database time into the past.

